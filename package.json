{
  "name": "personal-site",
  "version": "1.0.0",
  "description": "Personal website with blackjack trainer",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "check:agent-sync": "bash scripts/check_agent_skill_sync.sh",
    "setup:hooks": "bash scripts/install_git_hooks.sh"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
